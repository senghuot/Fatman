<!-- Fixed navbar -->
<div class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">FATMAN.</a>
    </div>
    <div class="navbar-collapse collapse">
      <form class="navbar-form navbar-left form-inline">
        
        <div class="input-group">

          <span class="input-group-btn">
            <select class="form-control selectpicker">
              <option>ALL LOCATIONS</option>
              <option>BATTAMBONG</option>
              <option>PHNOM PENH</option>
            </select>
          </span>

          <span class="input-group-btn">
            <input type="text" class="form-control" placeholder="Search..." />
          </span>
          
        </div>


        <!-- <input type="text" class="form-control" placeholder="Search..." />
        <select class="form-control ">
          <option value=0>City</option>
          <% for (var i = 0; i < locations.length; i++) { %>
            <option value="<%= locations[i]._id %>"><%= locations[i].city %></option>
          <% } %>
        </select>
        <input type="text" class="form-control" placeholder="location" id="geoLocation" />
        <input type='button' class='form-control btn-primary' id='getLocation' value='Get Location' /> -->
      </form>
      <ul class="nav navbar-nav navbar-right">
        <li class="<% if (typeof about != 'undefined') { %> <%= about %> <% } %>"><a href="/development">DEVELOPMENT</a></li>
        <li class="<% if (typeof post != 'undefined') { %> <%= post %> <% } %>">  <a href="/post">POST</a>
        </li>

        <!-- display log in options -->
        <% if (isAuthenticated) { %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= user.fname.toUpperCase() %><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><a href="/dashboard">Dashboard</a></li>
              <li><a href="/authenticate/logout">Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="<% if (typeof login != 'undefined') { %> <%= login %> <% } %>">  <a href="/authenticate/login">LOG IN</a>
        <% } %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>